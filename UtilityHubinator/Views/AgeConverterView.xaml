<UserControl x:Class="UtilityHubinator.Views.AgeConverterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UtilityHubinator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Width="100" Text="{Binding Years, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="1" Width="100" Margin="0,5,0,5"
                    Content="Convert" Command="{Binding ConvertCommand}" />
        <TextBlock Grid.Row="2" Width="200" Text="{Binding Seconds}" />
    </Grid>
</UserControl>
