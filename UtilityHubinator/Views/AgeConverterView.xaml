<UserControl x:Class="UtilityHubinator.Views.AgeConverterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UtilityHubinator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Calculator" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="200">
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Input:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding Years, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" x:Name="InputBox"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Output:" VerticalAlignment="Center" Margin="0,10,10,0"/>
            <TextBox Text="{Binding Seconds, Mode=OneWay}" Grid.Row="1" Grid.Column="1" x:Name="OutputBox" IsReadOnly="True" Margin="0,10,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Grid.Row="1" Width="100" Margin="0,5,0,5"
            Content="Convert" Command="{Binding ConvertCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
